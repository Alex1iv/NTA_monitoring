PATH_SECRETS: "secrets.yaml"
PATH_MODELS : "./models/"
PATH_DATA : "./data/"
PATH_FIGURES: "./figures/"
LOG_DIR: "./logs/"

random_seed: 42

ssh:
  ssh_host : "95.215.1.30"
  ssh_port: 22
  remote_host: "localhost"
  remote_port: 9000
  local_port: 12000

collector_db:
  host: "95.215.1.30"
  port: 6124
  dbname: "default"

callbacks:
  model_name: "lstm"
  cooldown_epochs: 1
  learning_rate: 0.001  
  min_learning_rate: 1e-6
  reduce_patience: 5
  stop_patience: 150
  monitor: "mse"
  verbose: 1
  mode: "max"
  save_weights_only: true
  save_best_only: true
  restore_best_weights: true
  factor: 0.9

torch_params:
  num_epochs: 60
  learning_rate: 0.001

intervals:
  ci_high: 11
  ci_low: 5.5
  ci_minimum: 1

intervals_country_ru:
  ci_high: 1.4
  ci_low: 1.5
  ci_minimum: 0.35
  feature_name: "country_ru"
  scaler_filename: "ru_scaler.gz"
  chkpoint_filename: "ru_tcn_checkpoint.pt"

intervals_country_F:
  ci_high: 1.5
  ci_low: 1.75
  ci_minimum: 0.35
  feature_name: "country_F"
  scaler_filename: "ru_scaler.gz"
  chkpoint_filename: "ru_tcn_checkpoint.pt"

pred_period_minutes: 1440
logging: true
verbose: true
